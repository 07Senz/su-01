Hidden member store used by the admin panel to persist login credentials.

Format (JSON):
{
  "members": [
    { "id": "G.M#123", "password": "memberPassword", "memberType": "G.M"|"E.M"|"Core" }
  ]
}

This file is intentionally simple and should be edited only via the admin UI.

