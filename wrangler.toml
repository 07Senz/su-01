name = "rud-stepup-nextjs"
main = "server.js"
compatibility_date = "2024-01-01"

[build]
# Cloudflare build is forced to fail so Cloudflare clearly shows build errors
command = "npm run build"



[vars]
# Used by app/api/members/admin/route.ts
ADMIN_PASS = "123"

